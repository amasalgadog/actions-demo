function sum(a, b, c) {
    return a + b + c;
}

module.exports = sum; // Exporta la funcion sum para que pueda ser utilizada en otros archivos
// Pueder ser importada en otros archivos utilizando require('./sum')... VER ARCHIVO sum.test.js